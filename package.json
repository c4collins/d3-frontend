{
  "name": "d3-frontend",
  "version": "0.0.0",
  "description": "A frontend for email (or any) information that allows for converting that data easily into a number of graphs generated by the d3 library so marketing professionals can build their own dashboard from the metrics provided by their ESP.  This does not include any of the backend data handling, just defines a structure for the data to be presented to the frontend, and the frontend itself.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Connor Collins",
  "license": "ISC",
  "dependencies": {
    "coffee-script": "^1.10.0",
    "jade": "^1.11.0",
    "stylus": "^0.53.0"
  },
  "repository": { 
    "type" : "git",
    "url" : "https://github.com/c4collins/d3-frontend.git"
  }
}
